# Ai写稿系统 —— 开发过程总结

> 本文记录「智能编辑系统」（内部代号：内容工坊 / Content Studio）从方案到上线的完整开发过程。
> 相关文档：[系统设计方案](design.md)（设计方案）｜[多人使用与后台管理方案](multi-user-and-admin-console.md)（多人使用设计稿）
> 记录日期：2026-09-10

---

## 一、项目速览

| 项目 | 内容 |
|---|---|
| 成品名称 | **智能编辑系统**（内部代号 内容工坊 Content Studio） |
| 访问地址 | **http://YOUR_SERVER_IP** |
| 部署环境 | YOUR_SERVER_IP · Ubuntu 26.04 LTS · 宝塔面板 · Nginx 80 + uvicorn(systemd) |
| 代码位置 | `/path/to/content-studio`（服务器直接开发） |
| 技术栈 | React 19 + React Flow(`@xyflow/react` 12) + Vite 8 + TS 6 + Zustand / FastAPI + SQLAlchemy 2.0 async + PostgreSQL 18 / DeepSeek（OpenAI 兼容协议） |
| 代码规模 | 后端 1247 行 Python（14 文件）· 前端 1334 行 TS/TSX（8 文件）+ 173 行 CSS |
| 提交记录 | 15 个提交（`6fe6ce0` → `72c778f`） |
| 开发时长 | 2026-09-09 09:26 → 2026-09-10 01:54（约 16 小时，含验证） |
| 验证方式 | Playwright 无头浏览器端到端自动化（15 个脚本，累计 40+ 项断言全通过） |

---

## 二、需求演进（三次修正）

**① 原始方案**（来自 Obsidian 方案笔记）
草稿输入 → AI 生成电视稿/报刊稿 → 人工审定 → 多平台新媒体稿 → **API 推送到各平台** → 人工审核发布

**② 技术选型验证后的三项修正**
| 原方案 | 修正为 | 原因 |
|---|---|---|
| Claude + GPT-4o 多模型混合 | **统一 DeepSeek** | Claude/OpenAI 官方 API 未对中国大陆开放；DeepSeek 中文新闻改写性价比高 |
| API 推送到各平台后台 | **生成最终成稿 + 人工复制粘贴发布** | 小红书无公开发布 API、微信公众号需认证、抖音需企业资质；混合方案不可维护 |
| Docker 自部署 | **宝塔面板直接部署** | 服务器已装宝塔，且 80 端口有系统 Nginx 在跑既有站点 |

**③ 使用过程中迭加的功能**
AI 审稿节点 → 节点级提示词编辑 → 工具节点（稿件精简 / 风格提取）→ 界面品牌与交互优化（可拖拽调宽、节点库视觉区分）

---

## 三、开发过程时间线

| 提交 | 内容 |
|---|---|
| `6fe6ce0` | Phase 1 后端骨架：FastAPI 结构 + requirements + .env |
| `084507d` | Phase 1 前端骨架：Vite + React + TS + @xyflow/react + zustand + axios |
| `a913bd2` | 部署配置入库：Nginx server 块 + systemd 单元 + 部署文档 |
| `4632f43` | **后端主体**：JWT 认证、项目 CRUD、画布节点/连线、执行引擎（改写/审定/导出）、WebSocket、DeepSeek（含无 Key 模拟模式）、工作流模板一键生成 |
| `56346dc` | **前端主体**：登录注册、项目管理、React Flow 无限画布、5 类自定义节点、状态色 + WS 实时、配置面板 |
| `86e2108` | 修复成稿预览被清空的 bug + 新增成稿预览区块 |
| `b464655` | 成稿预览下方增加一键复制按钮 |
| `b09a5d4` | 每个节点可自定义提示词与模型档位 |
| `e6045bd` | 提示词编辑器预填默认模板，支持在默认版上修改 |
| `90cdc16` | **AI 审稿节点**（可拖入、可替代人工审定） |
| `41b2099` | 导出节点补齐小红书/头条平台 |
| `6ed195f` | 品牌更名「智能编辑系统」+ 右侧栏可拖拽调宽 |
| `bd608d2` | 节点库视觉区分（分组色点 / 条目色带 / 终稿徽标） |
| `fe274fc` | **工具节点组**：稿件精简 / 风格提取 |
| `72c778f` | 多人使用与管理后台方案设计稿 |

---

## 四、关键设计决策（含理由）

1. **不做平台直推，改"成稿导出 + 人工粘贴"**
   调研确认小红书无公开第三方发布 API，微信需认证服务号、抖音需企业资质。改为生成最终成稿（来源稿预览 → 一键复制全文 → 下载 .md），把不可控的平台对接风险降到最低。

2. **无 API Key 时自动"模拟模式"**
   未配置 `DEEPSEEK_API_KEY` 时返回带「模拟模式」标注的版式化文本，保证流程可演示、可自动化测试；配置后自动切换真模型（实测无缝切换）。

3. **稿件内容池解析规则：直接上游优先 → 空则传递回溯**
   审定/AI 审稿节点本身不产出稿件，若只取直接上游会取不到内容。规则改为：先取直接上游；若直接上游无稿件，沿边向更上游回溯。

4. **风格提取产物做"元数据隔离"**
   风格提取节点输出 `format_type=style_prompt`，被审稿、成稿导出、内容池自动排除，不会被当成稿件处理；同时若它连到改写节点，其内容会作为「参考风格要求」注入下游提示词。

5. **AI 审稿默认"标准模式"而非严格模式**
   早期标尺过严，把一份正常稿以"标题与正文年份矛盾"为由打回（实为误判）。改为只对事实矛盾、夸大失实、敏感违规、严重语病等实质问题打回；重要稿件可开"严格模式"（评分<80 即打回）。

6. **精简节点做"二次强制压缩"**
   DeepSeek 首轮普遍删得不够（要求压到 1/3，实测只到 69%）。改为：首轮结果若超出目标字数 1.15 倍，则按明确字数上限再压（最多 2 次），并提供 1/4、1/3、1/2 三档，后端默认 0.35。

7. **节点级配置化**
   提示词、模型档位（deepseek-chat / deepseek-reasoner）、审稿严格模式、精简压缩比，全部存入节点 `config`（JSONB），做到"同类型节点可各不相同"。

---

## 五、最终功能清单

**画布与节点（7 类）**
- 草稿输入 · AI 改写（TV/报刊）· 人工审定 · 🤖 AI 审稿 · 新媒体转换（公众号/微博/抖音/小红书/头条）· 🧰 工具（稿件精简 / 风格提取）· 成稿导出（5 平台）

**执行与协作**
- 按连线拓扑执行；单节点执行 + 整画布"自动执行"（跳过需人工的审定节点）
- WebSocket 实时推送节点状态（待命/执行中/完成/失败/已通过），画布节点与连线动态着色
- 标准工作流模板一键生成（10 节点 10 连线），也可建空白项目自由拖拽连线
- 稿件 Revision 追溯链（parent_revision_id）+ 节点历史

**审稿**
- 人工审定：逐篇批注 / 通过 / 打回
- AI 审稿：自动判定 + 写回 `[AI审稿·模型] 意见（评分 xx·标准/严格）`；新建项目可勾选默认替换人工审定

**成稿与发布**
- 成稿导出节点：来源稿选择（点击即预览）、字数统计、一键复制全文、下载 .md
- 全程不调用任何平台 API，由用户复制后到平台后台发布

**界面**
- 三栏结构：左侧节点库（分组色点 + 条目色带 + 终稿徽标）· 中部无限画布 · 右侧配置面板（可拖拽调宽 260–780px，宽度记忆，双击复位）

---

## 六、踩坑与修复记录（有复用价值）

| # | 问题现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 前端构建报 `csstype/index.d.ts TS1005` | 首次 `npm install` 中断，类型文件被截断（19517 行处报 19518 行错） | `rm -rf node_modules` 干净重装 |
| 2 | `ReactFlow` 报 TS2786 无法作为 JSX 组件 | `@xyflow/react` v12 **无默认导出**，默认导入绑定到了模块对象 | 改为命名导入 `import { ReactFlow }` |
| 3 | systemd 服务反复 `activating` | 冒烟测试残留的 uvicorn 进程占着 8000 端口 | 清理残留进程后重启服务 |
| 4 | `sudo` 一直认证失败 | `echo 密码 \| sudo tee 文件 <<EOF`：**管道与 heredoc 争抢 stdin**，密码没送达 | 改为"先写临时文件 → `sudo cp`" |
| 5 | 转换节点报"缺少上游稿件内容" | 审稿节点不产稿，直接上游取不到内容 | 上游解析改为**传递性回溯** |
| 6 | 导出的成稿来源是报刊稿而非公众号稿 | 选稿规则偏好"最深处已审定稿"，忽略了直接上游 | 改为**直接上游优先**，空才回溯 |
| 7 | 成稿预览生成后被清空 | 生成后调用来源刷新，刷新函数里把预览状态一并重置 | 仅切换节点时重置；按 revision_id 回查服务端 |
| 8 | 提示词"已保存"提示一闪而没 | 同类问题：`syncNode` 触发 effect 又把 `saved` 置 false | 从刷新 effect 中移除该重置 |
| 9 | E2E 断言"假通过" | 提示词输入框与输出预览用了同一个 class，`querySelector` 选错元素 | 拆分为 `.out-content` / `.preview-content` |
| 10 | AI 审稿误打回正常稿 | 审稿提示词标尺过严（把"未写年份"当重大问题） | 务实化提示词 + 标准/严格双模式 |
| 11 | "夸大稿被 AI 审稿放行" | 实为**改写环节已清理夸大内容**，审稿对象是干净稿 → 非漏检 | 通过对比改写前后文确认，无需改代码 |
| 12 | 精简幅度不达标（要求 1/3 只到 69%） | 模型不愿删事实；且未点档位时后端无目标值 | 二次强制压缩 + 档位 + 后端默认 0.35 |
| 13 | 测试中节点"重叠"点不到 | 画布 fitView 放大 1.9 倍，卡片视觉变大后相邻（并非布局 bug） | 脚本改为先归位节点/适应视图再操作 |
| 14 | 无头浏览器读不到剪贴板 | Chromium 无头环境限制 readText（写入成功，toast 已证实） | 断言改为验证写入反馈 |

---

## 七、验证体系与结果

**方式**：Playwright + 无头 Chromium，直接驱动真实页面（非 mock 接口），真实 DeepSeek 调用。

**脚本清单**（`e2e/（自动化脚本目录）`）
`e2e.js`（主流程）· `ai_review_check / _pass / _reject`（AI 审稿三种结果）· `prompt_check / prompt_check2`（自定义提示词生效与非回显）· `prefill_check`（提示词预填/未保存提示/恢复默认）· `tool_check`（工具节点）· `custom_flow_check`（无模板自由流程）· `ui_check / palette_check / panel_check`（标题、标签、拖拽调宽、视觉区分）· `copy_check`（复制按钮）· `diag / tool_diag`（诊断）

**关键结果**

| 验证项 | 结果 |
|---|---|
| 全链路（注册→模板→草稿→TV/报刊改写→审定→转换→成稿→自动执行） | **12/12 通过，0 控制台错误** |
| 自定义流程（空白画布 + 拖入 3 节点 + 手动连 2 边 + 草稿直转公众号 + 成稿复制） | 全部通过 |
| AI 审稿 | 正常稿自动通过（评分 92·标准）；标题正文矛盾稿被打回并给出具体理由 |
| 自定义提示词 | 输出含自定义标记且**非提示词回显**（1328 字） |
| 工具节点 | 精简 185 字 → 67 字（保留全部关键事实）；风格提取 207 字可直接复用；风格注入下游正常出稿 |
| UI 调整 | 标题/品牌/标签/右侧栏拖拽 360→523px 记忆/双击复位 全部通过 |

---

## 八、部署与运维

**架构（单机）**
```
浏览器 → Nginx(80, server_name YOUR_SERVER_IP)
          ├── 静态：/path/to/content-studio/frontend/dist
          ├── /api/ → 127.0.0.1:8000 (uvicorn, systemd)
          └── /ws/  → 127.0.0.1:8000 (WebSocket 升级)
uvicorn → PostgreSQL 18（127.0.0.1:5432，库 content_studio）→ DeepSeek API（外网）
```

**配置文件**
- Nginx：`/etc/nginx/conf.d/content-studio.conf`（仓库 `deploy/nginx.conf.example`）
- 服务：`/etc/systemd/system/content-studio-backend.service`（开机自启 + 崩溃自动重启）
- 密钥：`backend/.env`（`DEEPSEEK_API_KEY`、`JWT_SECRET`、`DATABASE_URL`，已 gitignore）

**常用命令**
```bash
sudo systemctl restart content-studio-backend      # 后端重启
sudo systemctl status content-studio-backend       # 状态
journalctl -u content-studio-backend -f            # 实时日志
cd /path/to/content-studio/frontend && npm run build   # 前端构建
cd /path/to/content-studio && git log --oneline        # 版本历史
```

---

## 九、已知限制与待办

**安全问题（未修，建议优先）**
1. 🔴 WebSocket 仅校验 token、**不校验项目归属**（存在越权订阅）
2. 🔴 **注册完全开放**（局域网内任何人可注册并消耗 AI 额度）
3. 🔴 无 AI 用量统计与配额（费用不可控、不可分摊）

**功能缺口**
- 项目不能共享协作；无角色体系；无审计日志
- 节点库与提示词仍硬编码（管理后台无法管理，需先数据化）
- 无稿件版本对比、无回收站、无 HTTPS

> 以上已整理为方案：[多人使用与后台管理方案](multi-user-and-admin-console.md)（P0 安全与配额 → P1 管理后台 → P2 模板数据化与协作）

---

## 十、经验小结

1. **先核实、再动手**：服务器现状（宝塔/端口/已有站点）、第三方库的导出方式（xyflow v12）都靠实测确认，避免按印象编码。
2. **自动化 E2E 是最大杠杆**：本轮 15 个脚本让每次功能迭加都能回归验证，也多次抓出"假通过"的断言缺陷。
3. **React 高频陷阱**：把"切换节点时重置"和"数据刷新后同步"混在一个 effect 里，会出现"刚设置就被清掉"的诡异现象（本项目犯了两次）。
4. **命令链别用 `| tail && echo OK`**：管道会吞掉退出码，导致构建失败仍显示成功。
5. **对 AI 输出的质量要设兜底**：提示词约束不足时（删减不够、标尺过严），用"二次压缩""双模式"等工程手段补位。

---

## 附：试用速查

1. 打开 **http://YOUR_SERVER_IP** → 注册或登录（演示账号 `demo` / `<演示账号密码>`）
2. 「＋ 新建」→ 勾选模板（默认含 AI 审稿）→ 创建项目
3. 点「草稿输入」节点 → 右侧填标题/正文 → 保存草稿
4. 依次执行「AI 改写」→「AI 审稿」/「人工审定」→「新媒体转换」→「成稿导出」
5. 「成稿导出」：选来源稿（点击即预览）→ 一键复制全文 → 到平台后台粘贴发布
6. 任意 AI 节点可改提示词：「🧠 提示词设置」→ 修改（已预填默认模板）→ 保存

---

## 十一、P0 升级：多人使用（2026-09-10）

配套设计：[多人使用与后台管理方案](multi-user-and-admin-console.md)

**背景**：系统已有真实用户（demo、admin、dengtt、xml），需要开放给更多同事前必须先补齐安全与费用控制。

**升级前准备**：数据库双格式备份（`cs-20260910-0654.dump/.sql`）、代码 bundle、`.env` 备份、标签 `v0.1.0-pre-multiuser`；并修复了 备份目录 目录缺执行位导致 pg_dump 失败的问题。

**本次落地**（提交 `31c0430`，全部为新增变更，不动既有数据）
- 🔴→✅ **WebSocket 越权修复**：原先只验 token 不验归属，任何登录用户可订阅他人项目状态；现校验 token 版本 + 项目归属，越权关 4403
- 🔴→✅ **注册关闭，改邀请码制**：一次性邀请码（可设角色/有效期/作废），前端按 `register-mode` 自动显示邀请码输入框
- ✅ **登录限流**（5 次/5 分钟）+ 密码策略（≥8 位含字母数字）+ 改密/改角色使旧会话失效
- ✅ **AI 用量计量与配额**：`ai_usage` 记录每次调用的 token/耗时/估算成本；配额语义 空=继承(600/月)、0=禁用、>0=上限，超额拦截且不消耗额度
- ✅ **审计日志**：注册/登录/登录失败/建删项目/节点执行（含失败）/审稿/邀请码操作全部留痕
- ✅ **每日自动备份**：03:30 执行，保留 30 天
- ✅ **运维 CLI**：`scripts/admin.py`（提升管理员、生成邀请码、查看用户、开关注册）

**验证**：邀请码注册与复用拦截、限流 429、配额两种越限拦截、用量与审计落库、WS 越权 4403 与无效 token 4401、核心执行链路回归（真实 DeepSeek）全部通过。

**常用运维命令**
```bash
cd /path/to/content-studio/backend
.venv/bin/python scripts/admin.py invite --role editor --days 7 --note "给某同事"   # 发邀请码
.venv/bin/python scripts/admin.py users                                          # 查看用户与角色
.venv/bin/python scripts/admin.py promote <用户名>                                # 提升管理员
.venv/bin/python scripts/admin.py registration on|off                             # 临时开放/关闭注册
```

**遗留**：P2 模板数据化（节点库/提示词入库）与项目协作尚未实施；节点库与提示词目前仍是代码内置。

---

## 十二、P1 升级：管理后台（2026-09-10）

**入口**：`http://YOUR_SERVER_IP/admin`（顶栏「管理后台」按钮仅管理员可见；非管理员访问会提示需要权限）

**六个页面**（提交 `1363c0c`）

| 页面 | 关键能力 |
|---|---|
| 📊 概览 | 用户/项目/节点/稿件总量、今日与本月调用与成本、**预算占用百分比**、近 N 天失败数 |
| 👥 用户管理 | 改角色、启用/禁用、**配额调整**（留空=继承 / 0=禁用）、重置密码、**一键生成邀请码**，并显示每人项目数与近 30 天用量 |
| 📁 项目管理 | 全局项目列表 + 搜索、删除（级联）、转移归属 |
| 💰 用量看板 | 按日趋势、按用户排行、导出 CSV（Excel 可直接打开） |
| 📜 审计日志 | 按动作/用户筛选，含对象、详情、IP |
| ⚙️ 系统设置 | 注册开关、默认配额、月预算、并发上限 |

**设计要点**：管理接口全部要求管理员角色；改角色或禁用会**立即失效该用户旧会话**；不能禁用自己的账号；所有管理动作写审计。

**验证**：概览 8 卡片、用户列表 6 条并实测改角色生效、邀请码生成流程、项目列表 5 条、用量双表、审计 44 条、设置可编辑、非管理员被拒 —— 全部通过。过程中还修掉一个前端小问题（邀请码天数输入非法值会导致 422，已加参数兜底）。

---

## 十三、P2 升级：模板数据化 + 成稿排版（2026-09-10）

### 13.1 管理后台改为两栏式布局
左侧为**功能区**（7 个入口，带数量徽标与当前登录信息），右侧为**细节设置区**（标题 + 说明 + 内容）。
功能区：概览 / 用户管理 / 项目管理 / 用量看板 / 审计日志 / **节点与提示词** / 系统设置。

### 13.2 节点库与提示词数据化（提交 `293a151`）
- 新增 `node_templates`（17 条内置播种）与 `prompt_templates`（全站 + 个人模板）两张表
- 管理后台「🧩 节点与提示词」页：可改节点**名称/图标/颜色/排序/默认提示词/启停/删除/新增**；可编辑**全站提示词**（改完全站生效）
- 工作台节点库改为**从接口加载**（内置定义兜底），管理员改动即时生效
- 提示词编辑器新增**我的模板**（另存 / 载入 / 删除）
- 生效优先级：**节点 config.prompt > 全站模板 > 内置常量**（个人模板仅作个人库，不自动覆盖全局规范）
- 修复两个 bug：`/api/prompts` 曾返回内置常量（导致管理员改动在面板看不到）；管理接口 PATCH 后读 `updated_at` 触发 `MissingGreenlet` 500（已补 `db.refresh`）

### 13.3 成稿导出改为真正的排版处理（提交 `9f1fa43`、`4aa1266`）

**背景**：此前「成稿导出」只是把上游稿标记为成稿，**既没有提示词也不做排版**。

**现在**
- 执行导出时按**排版提示词**处理稿件：公众号输出**内联样式 HTML**（主标题居中 22px、导语浅灰 `#f7f7f7` 引用块带主色竖线、小标题左侧色块、正文 16px/行高 1.75），仅使用微信编辑器支持的样式属性
- 生成的成稿是**新的 Revision**（status=finalized，记录模型与 token 用量），计入配额并写审计
- 面板新增 **📋 一键复制富文本**：优先 `ClipboardItem(text/html)` → 回退 `contenteditable + execCommand` → 最终回退复制源码并提示用编辑器 HTML 模式；**可直接粘贴进秀米 / 微信编辑器并保留排版**
- 下载按钮按内容类型自动给 `.html` / `.md`；打开导出节点时**自动载入上次生成的成稿**
- 提示词可编辑：导出节点 → 🧠 提示词设置（已预填排版提示词）；或在管理后台改 `export_wechat` **全站生效**
- 提示词为空时回退为"直接成稿"（不调用 AI、不消耗额度）

**实测**：1559 字 Markdown → **5709 字内联样式 HTML**（`<h1 style="font-size:22px;font-weight:bold;text-align:center;…">`），面板 HTML 预览渲染正常、富文本复制走通、0 控制台错误。

**未做（用户决定）**：项目共享协作（设计备查于 [多人使用与后台管理方案](multi-user-and-admin-console.md)）。

### 13.4 微博成稿提示词重写（提交 `b74ec8f`）

**要求**：贴合微博编辑框排版规范，并附带话题引流。

**提示词要点**（`export_weibo`，564 字）
- **纯文本输出**：微博不解析 Markdown / HTML，禁止 `**`、`##`、```` ``` ````、`<p>`、`- 列表`（否则用户会看到乱码符号）
- **结构**：首行钩子（≤30 字，信息流只展示开头）→ 核心事实分段（每段 1~2 句）→ 互动引导
- **话题引流**：正文后独立一行 3~4 个 `#话题#`（前后都带 `#` 才会变成可点击话题），优先本地/垂类流量词，含 1 个政务或行业话题
- **账号引流**：结尾 `@官方账号`（缺省 `@云阳县融媒体中心`）
- 表情最多 2~4 个；正文 ≤180 字；不得新增事实或改动数字/人名/机构名

**实测输出**（186 字微博上游稿 → 223 字成稿）
```
汛期未至，练兵先行！💪

云阳县融媒体中心近日开展2026年防汛应急演练，模拟暴雨红色预警下的全媒体实战。

12名记者、2架无人机、1辆直播车齐上阵，多路连线实时回传，检验应急指挥与快速发稿能力。

演练后复盘明确：汛期24小时值班，重要稿件力争15分钟内首发，为全县防汛救灾提供及时舆论支持。

有备无患，为守土尽责的媒体人点赞！你怎么看？评论区聊聊👇

#防汛演练# #融媒体应急报道# #云阳在行动# #重庆身边事#

@云阳县融媒体中心
```
校验：无 Markdown/HTML 标记 ✅ · 4 个 `#话题#` ✅ · 含 @ 与互动引导 ✅ · 界面走纯文本预览 + 复制全文 ✅

**配套工具**：CLI 新增 `sync-prompt <key> [--apply]` —— 把代码内置提示词同步到数据库（默认只预览，加 `--apply` 才写入），便于以后调整内置提示词而不误覆盖管理员在后台的改动。

### 13.5 五个平台的导出提示词全部按平台规范重写（提交 `435ca8f`）

| 平台 | 字数/结构 | 话题格式 | 引流方式 | 禁止项 |
|---|---|---|---|---|
| **公众号** | 内联样式 HTML；主标题居中 22px / 导语浅灰引用块 / 小标题左侧色块 / 正文 16px·行高1.75 | 文内不挂话题 | 结尾引导关注 | class、外部 CSS（会导致粘进秀米掉样式） |
| **微博** | 正文 ≤180 字；钩子首行（≤30字）+ 事实分段 + 互动引导 | **双井号 `#话题#`** 3~4 个 | @官方账号 + 评论区引导 | Markdown/HTML（不解析，会显示乱码） |
| **小红书** | 标题 ≤20 字带 emoji；正文 300~600 字，emoji 小标题、第一人称口语 | **单井号 `#话题`** 5~8 个 | 收藏/评论引导 | 外链、联系方式 |
| **头条** | 导语 40~80 字独立成段；主体 600~1200 字，【】分层 | **双井号 `#话题#`** 1~3 个 | 关注账号 + 评论引导 | 第一人称口吻、夸张形容词 |
| **抖音** | 【发布文案】标题 ≤25 字 + 补充说明；【口播脚本】分镜 7 行（镜头｜画面｜口播｜字幕），前三秒钩子、总时长 30~60 秒 | **单井号 `#话题`** 3~5 个 | 片尾互动 + 关注引导 | 长定语书面语 |

> **话题格式的关键区别**：微博与头条用**双井号 `#话题#`**，抖音与小红书用**单井号 `#话题`**。
> 首轮实测时抖音误用了双井号（提示词写错），已修正为单井号并复验通过（5 个单井号话题、0 误用）。

**实测输出样例**

*小红书（362 字，标题 12 字，8 个单井号话题）*
```
前方记者画面实时回传
后方编辑同步处理素材
…
✨亲测感受
这才是真正的新闻铁军啊
为云阳融媒体的专业素养点赞
码住这份防汛应急流程

你们那边防汛演练怎么做的？评论区聊聊

#云阳 #重庆周边 #防汛知识 #融媒体中心 #防汛演练 #新闻工作者 #应急演练 #暴雨预警
```

*抖音（517 字，7 个分镜，5 个单井号话题）*
```
【发布文案】
暴雨红色预警！云阳融媒体中心全员出动
别慌，这是2026年防汛应急演练现场
#云阳 #融媒体 #防汛演练 #应急演练 #重庆云阳

【口播脚本】
镜头1｜画面：暴雨红色预警大屏特写，警报闪烁｜口播：暴雨红色预警！云阳融媒体中心全员出动！｜字幕：暴雨红色预警！全员出动！
…
```

*头条（406 字，【】分层，3 个双井号话题）*：导语 → 【演练规模与设备投入】→【演练流程与实战要求】→…→ 关注引导 + `#云阳防汛演练# #云阳县融媒体中心# #防汛应急#`

**验证方式**：脚本自动建临时项目（草稿 → 三平台转换 → 三平台排版导出）→ 校验 → 删除项目，全部通过；测试数据未留在正式项目中。

### 13.6 工具节点新增「PDF 版面提取」（提交 `f46de95`）

**用途**：把报纸/文件 PDF 里的稿件按**版面版块**提取出来，直接交给下游节点（AI 改写 / 审稿 / 新媒体转换）继续处理。

**操作流程**
1. 从节点库「工具」组拖入 **📄 PDF 版面提取** 节点
2. 面板点「上传 PDF 文件」（≤40MB，仅 PDF）
3. 系统自动解析并按版块列出：**第 N 版 · 第 X 栏 · 字数 · 识别到的标题 · 正文预览**
4. 勾选需要的版块（可全选/全不选，实时显示已选字数）→ 点「生成稿件」
5. 把该节点连到下游节点（改写/审稿/转换），稿件即作为输入流转

**解析引擎（`backend/app/pdf_tools.py`，PyMuPDF）**
- **合并相邻块**：同栏、同字号、纵向相邻的块合并（PDF 常把一行标题或一段正文拆成多个块）
- **正文字号判定**：取"按字数加权的众数字号"作为基准（比中位数稳——标题多时中位数会被带偏）；字号明显更大且较短的块判为**标题**
- **成稿规则**：按「页 → 栏 → 纵坐标」排序，标题块与其后同栏的正文块合并为一篇稿件
- **文本还原**：去掉中文之间因两端对齐产生的空格、按数字/标点边界续接被换行截断的句子

**接口**：`POST /nodes/{id}/pdf`（上传并解析）、`GET /nodes/{id}/pdf`（状态与版块）、`POST /nodes/{id}/pdf/select`（选用版块生成 Revision）、`DELETE /nodes/{id}/pdf`（移除）
**成本**：本地解析，**不调用 AI、不消耗配额**；生成的稿件状态为 rewritten，可正常参与下游流程
**清理**：删除节点或项目时自动清理上传目录（`uploads/` 已加入 .gitignore）

**边界处理（已实测）**
| 场景 | 响应 |
|---|---|
| 扫描图片版 PDF（无文字层） | 400「未从该 PDF 识别到文字（可能是扫描图片版）。需要 OCR 才能提取，请联系管理员。」 |
| 上传非 PDF 文件 | 400「只支持 PDF 文件」 |
| 未上传就执行节点 | 400「请先上传 PDF 文件并选择版块」 |

**实测结果**
- 后端：模拟报纸版面（两栏两篇）→ 正确识别 **2 篇**，标题为《云阳县融媒体中心开展防汛应急演练》《全县中小学开学第一课聚焦安全教育》，正文无断字
- 界面 9/9 通过：节点库出现 → 拖入画布 → 上传 → 解析摘要（1 页 / 271 字 / 2 版块）→ 版块列表 → 勾选计数联动 → 生成稿件 333 字 → 一键复制；0 控制台错误
- 下游联动：PDF 提取稿 → 公众号转换节点，正常输出 1676 字

**后续可选**：扫描版 OCR（tesseract / PaddleOCR）、多页报纸整版处理、版块顺序手工调整。

### 13.7 工具节点新增「新闻选题策划」（提交 `473ec54`）

**用途**：把上游草稿里的基本信息（时间、地点、主体、目标）变成**可落地的新闻选题方案**，供编辑选稿后继续走改写/转换/审稿流程。

**操作**：拖入「💡 新闻选题策划」节点 → 连上游草稿（或任一上游稿件）→ 点「策划新闻选题」→ 得到方案 → 把选定的选题复制到「草稿输入」节点继续流程（也可直接接改写节点）。

**输出结构**（提示词 418 字，可节点级修改或在管理后台改全站生效）
每条选题包含：**核心角度** · **目标受众与传播点** · **采访对象与必问问题**（2~3 类对象，各配关键问题）· **呈现形式与发布平台** · **时效与风险提示**；全部选题后给出 **【总体策划思路】**；素材信息不足时另附 **【需要核实的信息】**。要求只用素材已有事实、不得编造，纯文本不使用 Markdown。

**实测**（素材：云阳首届乡村美食节预告，真实 DeepSeek）
```
【选题一】首届云阳味道乡村美食节开幕
· 核心角度：从"首届"切入，关注首次以乡村美食节形式集中展示本地味道，侧重"第一次"的稀缺性
· 目标受众与传播点：本地居民、周边区县游客、美食爱好者；"首届"自带打卡与尝鲜属性
· 采访对象与必问问题：主办方（县商务委/文旅委）——为什么今年才办首届？对带动农产品销售有何预期？
  参展商家——带来哪些本地特色小吃？备货与预期客流如何？
· 呈现形式与发布平台：短视频+图文，融媒体中心新媒体平台及短视频账号
· 时效与风险提示：开幕前一周预热最佳；需核实"120 家商家"是否已最终确认
【选题二】滨江广场三天展销背后 …【选题三】非遗表演遇上乡村美食 …
【选题四】120家商家备战美食节 …【选题五】云阳味道能否带火乡村旅游
【总体策划思路】：以"首届云阳味道乡村美食节"为核心，形成开幕前预热（选题一、四）、
活动中体验（选题二、三）、活动后延伸（选题五）的完整报道链条。
```
**校验**：选题数量 5/5；核心角度、采访提问、呈现平台、时效风险、总体思路、无 Markdown —— **6 项全 PASS**。
**界面**：节点库出现 → 面板按钮「💡 策划新闻选题」→ 自动载入方案（1650 字）→ 一键复制；0 控制台错误。

**成本**：计入 AI 配额与用量（kind=`topic_plan`）；产物状态 rewritten，可作为下游节点输入。
**前端改动**：`ToolPanel` 泛化为按 subtype 渲染（稿件精简 / 风格提取 / 新闻选题策划），各自显示不同的说明、按钮文案、结果标题与后续用法提示。

### 13.8 工具节点新增「报道方案生成」（提交 `0b4e72c`）

**用途**：把上游素材（通常是「新闻选题策划」的结果，也可以是草稿）组合成一份**可直接分派执行**的报道方案。

**输出八段式结构**
```
【一、报道主题与定位】一句话主题、报道目的、目标受众、传播目标
【二、报道框架】预热期 / 活动期 / 收尾期，各阶段报道重点与发布节奏
【三、稿件清单】逐条：标题建议、核心内容要点、体裁与篇幅、发布平台、建议发布时间
【四、采访提纲】采访对象（单位/身份）+ 每个对象 3~5 个问题 + 需预约/协调事项
【五、人员与分工】记者、摄像、编辑、审核、发布
【六、物料与素材清单】图片、视频、数据、图表、历史资料
【七、风险与预案】核实要点、表述风险、舆情应对、现场安全
【八、需要核实的信息】
```

**实测**（真实 DeepSeek，链路：草稿 → 💡选题策划 → 📋报道方案生成）
方案输出如：预热期 9/18 发布"三天尝遍云阳乡村味道"导览 + 9/19 直播预告；活动期 9/20 开幕直播 + "120家商家参展背后"产业稿 + "滨江广场三天"街道观察 + "美食+非遗"文化稿；收尾期形成收官综述与二次分发；每篇稿件均给出体裁、篇幅、平台与发布时间。
校验 **10/10 PASS**：主题定位 / 报道框架 / 稿件清单 / 采访提纲 / 人员分工 / 物料清单 / 风险预案 / 待核实信息 / 无 Markdown / 篇幅 ≥1200 字。
界面 **6/6 PASS**：节点库出现、工具分组、画布节点、按钮「📋 生成报道方案」、自动载入方案、一键复制；0 控制台错误。

**用法提示**：方案中的「稿件清单」可拆成多个「草稿输入」节点分别走改写/转换；「采访提纲」可直接打印给记者。

> **顺带修复**（提交 `6177f11`）：从节点库拖入画布的新节点，此前显示的是原始类型名（如 `tool` / `rewriter`），
> 现已改为显示节点库里的可读名称（如「报道方案生成」）；历史节点也做了显示层兜底（回退为「工具 · 报道方案」形式）。

---

## 十四、稳定性强化：AI 重试与并发控制（提交 `8934216`、`cd8a6ad`）

**背景**：AI 调用此前没有重试也没有并发限制——DeepSeek 偶发超时/限流就让节点"莫名失败"；系统设置里的并发上限只有配置项、执行链路并未使用。

### 14.1 指数退避重试（覆盖所有 AI 调用）
统一收口在 `ai.py` 的 `_post_chat`（改写/转换/工具/审稿/排版导出全部经过它）：
- **3 次尝试**（1 次原始 + 2 次重试），退避 1s → 2s（带随机抖动），最大 8s
- **自动重试**：HTTP 429、5xx、请求超时、网络错误
- **不重试**：401/403（Key 无效）与其他 4xx，直接给出可操作提示
- **可读错误**：限流 / 超时 / 无法连接 / Key 无效，节点会显示真实原因（此前只显示"未知错误"）
- 用量记录新增 `retries` 字段，执行结果回传，界面提示里会显示"重试 N 次"

### 14.2 并发限制与排队（全局 + 单用户）
新增 `app/concurrency.py`：进程内并发限制器 + `ai_slot` 上下文管理器
- 读取系统设置：`max_concurrency_global`（默认 8）、`max_concurrency_user`（默认 2）、`max_queue_wait_seconds`（默认 120 秒）
- 超限时**排队等待**；排队超过 0.8 秒通过 WebSocket 推送 `queued` 状态 → 画布节点显示**「排队中」**（琥珀色）
- 排队超过上限 → 返回 429「AI 服务当前繁忙（已排队等待超过 N 秒），请稍后重试」，避免请求悬挂
- 管理后台「系统设置」可直接调整这三项

### 14.3 失败重试界面
节点执行失败时，配置面板显示红色提示条（含真实原因）+ **「↻ 重试执行」** 按钮，无需重新找节点/重新配置。

### 14.4 顺带修复的两个 bug
1. **失败原因丢失**：`HTTPException`（配额不足、缺上游、未上传 PDF 等）只回给前端，未写入节点记录，界面只能显示"未知错误" → 现已写入节点并推送
2. **配额无法清空**：管理接口把 `null` 当作"未提供"忽略，导致管理后台把配额输入框清空（想恢复"继承全局"）时实际不生效 → 改用 `model_fields_set` 判断显式传参

### 14.5 验证结果
| 验证 | 结果 |
|---|---|
| 单元：mock 服务返回 500×2 后成功 | PASS（3 次请求、retries=2、耗时 3.7s 含退避） |
| 单元：429 限流 | PASS（"AI 服务限流（已重试 2 次），请稍后重试"） |
| 单元：401 不重试 | PASS（仅 1 次请求，提示 Key 无效） |
| 单元：并发限制器 limit=1 | PASS（三个任务等待 0 / 0.4 / 0.8 秒串行） |
| 单元：并发限制器 limit=2 | PASS（两个立即执行） |
| 单元：排队超时 | PASS（触发超时，外层转 429） |
| 端到端：并发上限=1 两个并发执行 | PASS（第二个 `queued_ms=10375`，两者均成功） |
| 端到端：配额=0 → 失败提示 → 恢复 → 点重试 | PASS（提示真实原因；重试后节点=完成，服务端生成 1663 字稿件） |
| 控制台错误 | 0 |
