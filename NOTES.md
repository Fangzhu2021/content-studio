# 注意事项与已知限制（NOTES）

上线前请通读。标注 🔴 的为**必须处理**项。

---

## 1. 安全 🔴

| 项 | 说明 |
|---|---|
| `JWT_SECRET` | 必须替换为足够长的随机串；泄露等于任何人可伪造登录态 |
| `DEEPSEEK_API_KEY` | 只放服务器 `backend/.env`（已在 `.gitignore` 中）；**不要**提交仓库、不要写进前端；一旦在聊天记录/截图中出现过，建议到控制台作废重建 |
| 公开注册 | 默认**已关闭**（改用邀请码）。如需临时开放：`scripts/admin.py registration on`，用完请关闭 |
| 邀请码 | 一次性、可设角色与有效期；发给谁就发带码链接，泄露或误发可在 `/admin` → 用户管理里**作废** |
| 密码策略 | ≥8 位含字母与数字；管理员可重置（重置后该用户旧会话立即失效） |
| API 文档 | FastAPI 默认开放 `/docs` 与 `/openapi.json`（无需登录）。公网环境建议关闭或加访问控制 |
| 权限边界 | 所有业务接口都校验"项目归属当前用户"；WebSocket 亦校验 token 版本与项目归属（越权会返回 4403 关闭） |
| 传输安全 | 建议启用 HTTPS。非安全上下文下，浏览器的富文本剪贴板 API 可能受限（表现为"复制后粘贴只有纯文本"） |

## 2. 数据与备份 🔴

- **每日备份**：`pg_dump`（自定义格式）+ `.env` 备份，建议保留 30 天并放一份异地副本。
- **务必定期演练恢复**：把备份恢复到临时库验证可用——没有验证过的备份不算备份。
- **数据增长**：`revisions`（每次执行都新增稿件版本）、`ai_usage`、`audit_logs` 会持续增长。建议按需制定保留策略（例如审计留 12 个月、稿件版本保留最近 N 版），并监控库体积。
- 删除项目/节点会**级联删除**其画布数据与稿件记录（上传的 PDF 文件也会一并清理），操作前请确认。

## 3. 资源与成本

| 项 | 现状与建议 |
|---|---|
| AI 配额 | 按用户月度调用次数限制（空=继承全局默认 600 次/月、**0=禁用该账号**、>0=本月上限）。超额在调用前拦截，不消耗额度 |
| 全局预算 | `/admin` → 系统设置里的"全局月度预算"仅用于看板进度提醒，**不会自动阻断**；如需硬性止损请配合用户配额 |
| 并发与排队 | 全局并发（默认 8）、单用户并发（默认 2）、排队等待上限（默认 120 秒）。排队中节点显示「排队中」；超过等待上限返回「服务繁忙」 |
| 上传文件 | 单个 PDF ≤ 40MB；**未限制累计总量**，请监控磁盘并定期清理 `backend/uploads/`（删除节点/项目会自动清理对应目录） |
| 成本估算 | 用量看板的成本为**估算值**（单价写在 `backend/app/usage.py`，需按 AI 服务商最新价格调整） |

## 4. 内容合规与流程

- 本系统**不调用任何平台发布 API**：微信/抖音/小红书等平台的发布接口有资质与合规门槛，系统只产出成稿，由**人工复制到平台后台发布**。
- **AI 生成内容必须经审稿后发布**。系统提供人工审定（批注/通过/打回）与 AI 审稿（自动判定 + 意见，支持严格模式），但不能替代编辑责任。
- **审计留痕**：注册、登录、建删项目、节点执行（含失败）、审稿、邀请码与设置变更均有记录，可用于复盘与追责。
- AI 提示词已约束"不得编造事实、不得改动数字/人名/机构名"，但**仍需人工核对关键信息**（时间、地点、数据、称谓）。

## 5. 已知限制

| # | 限制 | 说明/后续 |
|---|---|---|
| 1 | **扫描图片版 PDF 无法提取** | 无文字层，需要 OCR（PaddleOCR / tesseract）才能支持；当前会明确提示"需要 OCR" |
| 2 | WebSocket 与登录限流为**进程内实现** | 单机部署无碍；若要跑多进程/多实例，需改为 Redis 房间与计数 |
| 3 | 无回收站 | 删除即永久删除（数据库级删除）；建议重要项目先导出或用测试项目验证流程 |
| 4 | 无稿件版本对比视图 | 数据层有 `parent_revision_id` 追溯链，界面暂未提供并排 diff |
| 5 | 无移动端适配 | 三栏画布布局面向桌面端；手机端仅能勉强查看 |
| 6 | PDF 版块切分偏启发式 | 按栏位 + 字号判标题；跨栏大标题、图文混排等复杂版式可能需要手工调整所选版块 |
| 7 | 提示词与节点库"双份维护" | 代码内置定义用于首次播种与兜底，数据库为准；改动内置提示词后用 `scripts/admin.py sync-prompt <key> --apply` 同步（默认只预览，避免覆盖后台改动） |
| 8 | 无 LICENSE | 如需开源请自行添加 |

## 6. 维护建议

```bash
# 服务与日志
systemctl status content-studio-backend
journalctl -u content-studio-backend -f

# 前端改动后必须重新构建
cd frontend && npm run build

# 后端改动后重启
sudo systemctl restart content-studio-backend

# 健康检查与冒烟
curl -s http://127.0.0.1:8000/api/health
curl -s http://YOUR_SERVER_IP/api/health

# 常用运维
cd backend && .venv/bin/python scripts/admin.py users
cd backend && .venv/bin/python scripts/admin.py invite --role editor --days 7
```

**升级前**：备份 → 更新代码 → 装依赖 → 构建前端 → 重启 → 跑一遍验收清单（见 [DEPLOY.md](DEPLOY.md) 第 7 节）。
**建议监控**：进程存活、磁盘余量（含 `uploads/` 与数据库体积）、备份任务是否成功、AI 调用失败率（`/admin` 用量看板）。
